界面：
聊天界面是否简洁、美观
聊天界面是否完整、没有错别字
聊天界面排版是否合理

功能：
是否可以向不同对象（如用户、公众号、群聊等）发送消息
是否可以接收不同对象发送来的消息
当发送消息失败时是否会有红色感叹号提示
是否可以连续多次发消息
输入框是否支持复制粘贴
发送出去的消息在两分钟内是否可以撤回
输入为空时是否可以点击发送
输入长度是否有限制
输入内容是否有限制（如特殊字符，各国语言，颜文字等）
是否可以发送表情、红包
是否可以从本地选择图片发送给对方
是否可以发送文件
是否可以转发/删除历史聊天记录
是否可以语音转换文字
是否可以查看历史聊天消息

性能：
在弱网状态下是否能接收/发送消息
断网时能否发送/接收消息
手机内存不足时是否还能接收/发送消息

兼容性：
使用不同的微信版本是否能互相发送消息
安卓用户是否能和iOS用户互相发送消息
使用不同手机型号是否能互相发送消息

安全：
聊天信息是否会被泄露
对方异地登录是否会有提示

易用性：
是否支持发送语音
是否支持拨打语音/视频电话